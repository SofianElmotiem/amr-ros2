twist_mux:
  ros__parameters:
    topics:
      navigation:
        topic   : cmd_vel
        timeout : 0.2
        priority: 10
      joystick:
        topic   : cmd_vel_joy
        timeout : 0.2
        priority: 100